name: Elixir CI

on: push

jobs:
  checks:
    runs-on: ubuntu-latest

    container:
      image: elixir:1.9.4-alpine

    services:
      dgraph:
        image: dgraph/standalone
        ports:
          - '8001:8000'
          - '8081:8080'
          - '9081:9080'
        options: --health-cmd "curl -sSf http://localhost:8080/health > /dev/null" --health-interval 5s --health-timeout 5s --health-retries 5

    steps:
    - uses: actions/checkout@v1

    # Caching
    - name: Dependencies cache
      uses: actions/cache@v1
      with:
        path: deps
        key: deps-${{ hashFiles('mix.lock') }}
    - name: Build cache
      uses: actions/cache@v1
      with:
        path: _build
        key: build-${{ hashFiles('mix.lock') }}

    # Setup
    - name: Install dependencies
      run: |
        mix local.rebar --force
        mix local.hex --force
        mix deps.get

    # Checks
    - name: Compiler warnings
      run: |
        mix compile --warnings-as-errors --force
        WARNINGS_AS_ERRORS=true mix test
      env:
        DGRAPH_HOST: dgraph
    - name: Code format
      run: mix format --check-formatted --dry-run
    - name: Code conventions (Credo)
      run: mix credo
    - name: Test suite
      run: mix test
      env:
        DGRAPH_HOST: dgraph
    - name: Static code analysis (Dialyzer)
      run: mix dialyzer
